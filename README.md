# CS179M_P2
K-Means algorithm using nearest neighbor to find the shortest method to travel through points where the points are clustered up.

## Instructions
To Run: \
  1. Run the file `ComputePossibleSolutions.py` in the terminal in the project directory \
  2. Enter the fille such as `Almond9832.txt`, input files are included \
